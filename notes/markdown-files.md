# Requirements of Markdown

*   Use {: .omit} after header and paragraph nodes to prevent the node from
    being added to the search database.

*   Frontmatter data referenced in header and paragraph nodes will be
    expanded. e.g. {{page.title}}

