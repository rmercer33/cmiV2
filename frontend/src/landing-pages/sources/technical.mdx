# Custom Technical Source Landing Page

This is a **custom landing page** override for the `technical` spiritual source.

It is rendered using **MDX** from `src/landing-pages/sources/technical.mdx`!

### Resources available:
- **Style Guides**
- **Markdown syntax tips**

We can even embed custom styles and React layouts here. Graceful fallback means any other source (like `concept`) will continue to render its standard auto-generated directory cards.